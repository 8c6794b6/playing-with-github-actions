{
  "name": "ps01",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf output dist",
      "build": "spago bundle-app -t output/index.js",
      "dist": "ncc build -m output/index.js",
      "all": "yarn build && yarn dist"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0",
    "purescript": "^0.14.0",
    "spago": "^0.19.1"
  }
}
