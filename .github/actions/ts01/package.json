{
  "name": "ts01",
  "version": "1.0.0",
  "description": "",
  "main": "output/main.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "ncc build -m",
    "clean": "rm -rf output dist",
    "all": "tsc && ncc build -m"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}
